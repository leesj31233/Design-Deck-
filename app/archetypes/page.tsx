import Link from "next/link";
import { ArrowUpRight, BookOpen, Gauge, Images, Sparkles } from "lucide-react";
import { archetypes } from "@/lib/archetypes";
const icons={knowledge:BookOpen,engineering:Gauge,editorial:Images,motion:Sparkles};
export default function ArchetypesPage(){return <main className="mx-auto min-h-screen max-w-6xl px-5 py-12"><div className="max-w-3xl"><div className="text-xs font-semibold uppercase tracking-[.18em] text-[var(--muted)]">Design Deck / Archetypes</div><h1 className="mt-4 text-5xl font-semibold tracking-[-.055em]">One engine. Different professional design languages.</h1><p className="mt-5 max-w-2xl text-sm leading-7 text-[var(--muted)]">Each archetype has its own density, typography, interaction and visual rhythm. They share primitives, accessibility and engineering quality, but do not collapse into one generic style.</p></div><div className="mt-12 grid gap-4 md:grid-cols-2">{archetypes.map(a=>{const Icon=icons[a.id];return <Link href={a.href} key={a.id} className="dd-glass group rounded-[22px] p-6 transition hover:-translate-y-1"><div className="flex items-start justify-between"><div className="grid size-11 place-items-center rounded-2xl bg-[var(--foreground)] text-[var(--background)]"><Icon className="size-5"/></div><ArrowUpRight className="size-5 text-[var(--muted)] transition group-hover:translate-x-1 group-hover:-translate-y-1"/></div><div className="mt-8 text-[10px] font-semibold uppercase tracking-[.13em] text-[var(--muted)]">{a.eyebrow}</div><h2 className="mt-2 text-2xl font-semibold tracking-[-.04em]">{a.name}</h2><p className="mt-3 text-sm leading-6 text-[var(--muted)]">{a.description}</p><div className="mt-5 flex flex-wrap gap-2">{a.principles.map(p=><span key={p} className="rounded-full border border-[var(--line)] bg-black/[.025] px-2.5 py-1 text-[10px] text-[var(--muted)] dark:bg-white/[.035]">{p}</span>)}</div></Link>})}</div></main>}
