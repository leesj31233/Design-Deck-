"use client";
import * as React from "react";
import { FileText } from "lucide-react";
import { cn } from "@/lib/utils";

export interface DocumentPage { id: string; page: number; thumbnail?: React.ReactNode; label?: string; annotations?: number; }
export function DocumentRail({ pages, activePage, onPageSelect, className }: { pages: DocumentPage[]; activePage: number; onPageSelect?: (page:number)=>void; className?: string }) { return <aside className={cn("dd-glass dd-scrollbar h-full min-h-[540px] w-[122px] overflow-y-auto rounded-[18px] p-2", className)}><div className="space-y-2">{pages.map(item=>{ const active=item.page===activePage; return <button key={item.id} onClick={()=>onPageSelect?.(item.page)} className="group block w-full text-left outline-none"><div className={cn("relative aspect-[3/4] overflow-hidden rounded-[10px] border bg-white shadow-sm transition group-hover:-translate-y-0.5 dark:bg-neutral-900", active ? "border-[var(--accent)] ring-2 ring-[var(--accent)]/15" : "border-[var(--line)]")}>{item.thumbnail ?? <div className="grid h-full place-items-center text-[var(--muted)]/55"><FileText className="size-6"/></div>}{item.annotations ? <span className="absolute right-1.5 top-1.5 grid min-w-4 place-items-center rounded-full bg-[var(--accent)] px-1 py-0.5 text-[8px] font-bold text-white">{item.annotations}</span>:null}</div><div className={cn("mt-1.5 flex items-center justify-between px-1 text-[9px]", active ? "font-semibold text-[var(--foreground)]" : "text-[var(--muted)]")}><span>{item.label || `Page ${item.page}`}</span><span>{item.page}</span></div></button>;})}</div></aside>; }
